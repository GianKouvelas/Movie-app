import { SearchValidator } from './search-validator';

describe('SearchValidator', () => {
  it('should create an instance', () => {
    const directive = new SearchValidator();
    expect(directive).toBeTruthy();
  });
});
